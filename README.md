# snek
no step on snek. (snake for the browser :) )

```
yarn dev // runs dev server with automatic reload on file changes
yarn watch // watches and recompiles on file changes (no dev server)
yarn build // one-time build
yarn test
yarn flow // to run a flow check (not required for `test` to pass
```